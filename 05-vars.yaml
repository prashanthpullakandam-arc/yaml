- name: task level variables
  hosts: local
  connection: local
  vars:
    cource: "dev ops with aws"
    Tutor: "prashanth"
    tenure: "devops 123"
  tasks:
  - name: varibales required
    vars:
      cource: "dev ops with azure"
    ansible.builtin.debug:
      msg: "hi how areu who is teaching {{ cource }}"
  